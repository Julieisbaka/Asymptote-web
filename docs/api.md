# API guide

This guide is split into focused topic pages so each API area is easier to
find and maintain. For exact TypeScript interfaces, see the
[type reference](types.md).

## Runtime and rendering

- [Runtime and engine setup](api/runtime.md)
- [Rendering and browser integration](api/rendering.md)

## Output helpers

- [PDF export](api/pdf.md)
- [Standalone EPS/PS conversion](api/conversion.md)
- [Utility helpers](api/utilities.md)

The package's main entry is `asymptote-web`. Optional features are exposed by
the `asymptote-web/pdf` and `asymptote-web/utils` subpaths. The topic pages
contain usage examples, options, browser constraints, and output details.
