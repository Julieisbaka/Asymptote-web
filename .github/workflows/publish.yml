name: Publish Package

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-publish
  cancel-in-progress: false

jobs:
  version:
    name: Read and verify package version
    runs-on: ubuntu-latest
    outputs:
      package-version: ${{ steps.version.outputs.package-version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false

      - name: Read package version
        id: version
        run: node -e "console.log('package-version=' + require('./package.json').version)" >> "$GITHUB_OUTPUT"

      - name: Verify version is unpublished
        run: |
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          PACKAGE_VERSION=$(node -p "require('./package.json').version")

          set +e
          npm view "${PACKAGE_NAME}@${PACKAGE_VERSION}" version --fetch-retries=0 --loglevel=error >/dev/null 2>npm-view-error.log
          status=$?
          set -e

          if [ "$status" -eq 0 ]; then
            echo "::error::${PACKAGE_NAME}@${PACKAGE_VERSION} is already published. Commit a new version before publishing."
            exit 1
          fi

          if ! grep -qE "E404|404 Not Found" npm-view-error.log; then
            cat npm-view-error.log >&2
            echo "::error::Unable to verify whether ${PACKAGE_NAME}@${PACKAGE_VERSION} is published."
            exit "$status"
          fi

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          package-manager-cache: true

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

  wasm:
    name: Build WASM runtime
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build WASM runtime
        run: bash ./wasm/build.sh

      - name: Upload WASM build output
        uses: actions/upload-artifact@v4
        with:
          name: wasm-dist
          path: dist
          retention-days: 1
          if-no-files-found: error

  vite-build:
    name: Build package
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          package-manager-cache: true

      - name: Install dependencies
        run: npm ci

      - name: Build package
        run: npm run build

      - name: Upload package build output
        uses: actions/upload-artifact@v4
        with:
          name: vite-dist
          path: dist
          retention-days: 1
          if-no-files-found: error

  test-and-pack:
    name: Test and pack package
    needs: [version, lint, wasm, vite-build]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Download WASM build output
        uses: actions/download-artifact@v4
        with:
          name: wasm-dist
          path: dist

      - name: Download package build output
        uses: actions/download-artifact@v4
        with:
          name: vite-dist
          path: dist

      - name: Verify package assets
        run: npm run verify:package

      - name: Run tests
        run: node --test --test-concurrency=1 test/*.test.js

      - name: Verify package metadata
        run: |
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          test "${PACKAGE_NAME}" = "asymptote-web"
          test "${PACKAGE_VERSION}" = "${{ needs.version.outputs.package-version }}"

      - name: Create tested package tarball
        run: npm pack --ignore-scripts

      - name: Verify package tarball contents
        run: |
          PACKAGE_TARBALL="asymptote-web-${{ needs.version.outputs.package-version }}.tgz"
          for required_file in \
            package/dist/asymptote-web.js \
            package/dist/asymptote.wasm \
            package/dist/asy.data \
            package/dist/asygl.js \
            package/dist/index.d.ts \
            package/dist/asymptote-js.d.ts; do
            if ! tar -tzf "$PACKAGE_TARBALL" "$required_file" >/dev/null 2>&1; then
              echo "::error::Package tarball is missing ${required_file}"
              exit 1
            fi
          done

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: asymptote-web-package-${{ needs.version.outputs.package-version }}
          path: asymptote-web-${{ needs.version.outputs.package-version }}.tgz
          retention-days: 7
          if-no-files-found: error

  publish:
    name: Publish to npm
    needs: [version, test-and-pack]
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - name: Configure Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false

      - name: Download tested package tarball
        uses: actions/download-artifact@v4
        with:
          name: asymptote-web-package-${{ needs.version.outputs.package-version }}
          path: .

      - name: Verify package tarball
        run: npm install --ignore-scripts --dry-run ./asymptote-web-${{ needs.version.outputs.package-version }}.tgz

      - name: Publish package
        run: npm publish ./asymptote-web-${{ needs.version.outputs.package-version }}.tgz --provenance
